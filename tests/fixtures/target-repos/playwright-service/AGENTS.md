# Playwright service fixture

This tracked directory is immutable test input. Pulse tests copy it to an
external temporary repository before installing dependencies, building the app,
starting the server or writing `.pulse` state.
