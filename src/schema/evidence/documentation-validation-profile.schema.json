{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Pulse documentation validation receipt payload v2",
  "description": "Registry-aware documentation validation payload binding canonical document identity, revision, verification profile, path and content hash.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "payload_version",
    "documents"
  ],
  "properties": {
    "payload_version": {
      "const": 2
    },
    "documents": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "document_id",
          "document_revision",
          "verification_profile",
          "path",
          "content_hash",
          "result"
        ],
        "properties": {
          "document_id": {
            "type": "string",
            "pattern": "^DOC-[A-Z0-9][A-Z0-9-]{2,63}$"
          },
          "document_revision": {
            "type": "integer",
            "minimum": 1
          },
          "verification_profile": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          },
          "path": {
            "type": "string",
            "minLength": 1
          },
          "content_hash": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$"
          },
          "result": {
            "const": "passed"
          }
        }
      }
    },
    "checks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["kind", "result"],
        "properties": {
          "kind": {"type": "string", "minLength": 1},
          "result": {"const": "passed"},
          "artifact": {
            "type": ["string", "null"],
            "pattern": "^sha256:[0-9a-f]{64}$"
          }
        }
      }
    }
  }
}
